[project]
name = "hand2notes-ingestion"
version = "0.1.0"
description = "Image import, ordering, format validation, and HEIC conversion"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "Pillow>=10.3",
    "pillow-heif>=0.16",
    "hand2notes-core-models",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hand2notes"]
