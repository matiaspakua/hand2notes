# Golden Fixture: Basic Flowchart

Place a `input.jpg` here containing a hand-drawn flowchart with:
- A start node
- A decision diamond with yes/no branches
- Action boxes on each branch
- An end node

The `expected-plantuml.puml` contains the expected PlantUML reconstruction.
