# Golden Fixture: Simple 3×3 Table

Place a `input.jpg` here containing a hand-drawn 3×3 table with:
- Column headers: Name, Score, Grade
- 3 data rows with student names, scores, and letter grades

The `expected-markdown.md` contains the expected GFM table reconstruction.
